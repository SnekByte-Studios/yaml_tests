---
name: Artifact tests
on:
  push:

jobs:
  upload-artifacts:
    name: keke
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        run: |
          echo value1 > art1.txt
      - name: uploadme
        uses: actions/upload-artifact@v4
        with:
          name: uploading
          path: art1.txt

  download-artifacts:
    needs: upload-artifacts
    runs-on: ubuntu-latest
    uses: actions/download-artifact@v4
    steps:
      - name: Download an artifact
        with:
          name: downloadme
        run: |
          value=`cat art1.txt`
          echo $value
...
